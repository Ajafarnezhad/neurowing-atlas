# Changelog — 3.1.1 Codex Clean

- حرکت مگس بازنویسی شد: steering/speed نرم، ترمز نزدیک هدف، برخورد پایدار و اثر فیزیکی باد.
- Training و `آزمون بدون مربی` جدا شدند.
- Online imitation learning با مدل پایدار و قابل ذخیره اضافه شد.
- وزن‌ها و history هر Task در LocalStorage ذخیره می‌شوند.
- محیط شروع/پایان هر Episode در history ثبت می‌شود.
- نور، غذا، بو و حرارت Drag می‌شوند؛ لمس مستقیم روی مگس؛ باد شدت/جهت دارد.
- ۱۵ Task واضح جایگزین لیست شلوغ قبلی شدند.
- Figure 8، قلب، حرف A و `love you Ali` با Trail پرواز رسم می‌شوند.
- Arena بزرگ‌تر، Fly animation و Trail واضح‌تر شدند.
- SWC واقعی نورون‌های نگاشت‌شده در حین Task خوانده و بر اساس activity مدل روشن می‌شود.
- نمای ساده پیش‌فرض است؛ ابزارهای تخصصی پشت `نمای تخصصی` باقی مانده‌اند.
- Script order اصلاح شد: `atlas.js → flight.js → lab.js → app.js`.
- تست Node برای ۱۵ Task اضافه شد.
